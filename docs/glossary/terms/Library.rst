.. _Library:

Library
===========

A directory that contains commands that are needed to run a program. Usually the library for a program is found in the "bin" folder of that directory, such as $FSLDIR/bin.
